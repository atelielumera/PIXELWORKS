{
  "buildCommand": "prisma generate && (prisma db push --accept-data-loss || echo 'db push skipped') && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "regions": ["gru1"]
}
